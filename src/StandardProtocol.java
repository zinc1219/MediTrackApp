public class StandardProtocol implements TreatmentProtocol {
    @Override public String getProtocolDescription() {
        return "Standard protocol — observation and medication as required";
    }
}
